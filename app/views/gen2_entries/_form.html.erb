<%= form_with(model: gen2_entry) do |form| %>
  <% if gen2_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gen2_entry.errors.count, "error") %> prohibited this gen2_entry from being saved:</h2>

      <ul>
        <% gen2_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :save_file %>
    <%= form.file_field :save_file %>
  </div>

  <div class="field">
    <select name="gen2_game">
      <option value="gold">Gold</option>
      <option value="silver">Silver</option>
      <option value="crystal">Crystal</option>
    </select>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
