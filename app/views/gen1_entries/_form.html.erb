<%= form_with(model: gen1_entry) do |form| %>
  <% if gen1_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gen1_entry.errors.count, "error") %> prohibited this gen1_entry from being saved:</h2>

      <ul>
        <% gen1_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :saveFile %>
    <%= form.file_field :saveFile %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
